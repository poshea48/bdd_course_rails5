<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% flash.each do |name, msg| %>
        <div class="text-center alert alert-<%= name %>">
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </div>
</div>